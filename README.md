# Meridian & Bean React App

This Vite app renders the existing `CoffeeBar.jsx` component from the parent `Downloads` folder.

## Run it

```bash
npm install
npm run dev
```

Build for production with `npm run build`.
